import {
  __async
} from "../chunk-MLCSNVBT.mjs";
import { genkitPlugin } from "genkit/plugin";
import { getDerivedParams } from "../common/index.js";
import { vertexAiRerankers } from "./reranker.js";
function vertexAIRerankers(options) {
  return genkitPlugin("vertexAIRerankers", (ai) => __async(this, null, function* () {
    const { projectId, location, authClient } = yield getDerivedParams(options);
    yield vertexAiRerankers(ai, {
      projectId,
      location,
      authClient,
      rerankOptions: options.rerankers.map(
        (o) => typeof o === "string" ? { model: o } : o
      )
    });
  }));
}
export {
  vertexAIRerankers
};
//# sourceMappingURL=index.mjs.map